# Models

本目录保存软件演示所需的模型标签和已检查的小型 ONNX 模型。

包含：

- `best.onnx`：从本地训练输出中筛选的小型 ONNX 模型，约 5.89 MiB。
- `labels.txt`：类别标签。
- `labels_cn.json`：中文类别映射。
- `model_source_path_note.md`：模型来源路径和上传策略说明。

不包含：

- 大型训练数据集。
- 完整 `runs/` 输出。
- 未确认的大模型文件。
- 真实密钥或环境配置。
